seteuid      设置有效用户id，可以用来实现身份降级

工作中学习最快的是去官网

pkill 批量杀进程

fork：子进程是父进程的copy（复制了pcb结构体）,独有的主要是进程号、资源使用、计时器
execl：原有程序代码段被新程序覆盖掉

开机时要运行的脚本在 /etc/rc.local
如只想让特定用户运行则放到  ~/.bashrc


标准流管道
拉起一个进程，该进程的标准输入或者标准输出会被重定向

