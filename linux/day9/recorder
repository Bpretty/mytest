任何虚拟地址通过页表页目录机制转换为物理地址
较少的物理内存实现虚拟地址的翻译

32位示例:
11 11   1010    11 00   
进程的pgd，指向页表目录的起始地址(物理地址)
不同进程的pgd肯定不同

相同的虚拟地址可以指向不同的物理地址    
    只要倒数第二列位置(图在心中)存的指向的页的首地址不同就行
不同虚拟地址可以指向同一个物理地址
    只要倒数第二列位置(图在心中)存的指向的页的首地址相同就行

通过快表可以加快物理寻址


